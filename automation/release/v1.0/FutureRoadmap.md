# Future Roadmap (v1.1)

1. **Performance Telemetry**: Injecting Lighthouse metrics for Web and `dumpsys meminfo` for Mobile to track battery/CPU drain during tests.
2. **iOS Support**: Expanding `DevicePool` and `CompatibilityManager` to cover XCUITest bindings for iPhones.
3. **API Load Testing**: Introducing a purely headless `API Execution Engine` for backend bombardment.
