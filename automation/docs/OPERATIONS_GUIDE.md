# Operations Guide

## Managing Datasets
When the QA environment database is wiped, the `automation/mobile/data/users/validUsers.json` file must be updated to match the new seed DB state.

## Modifying CI Matrix
To modify CI workflow settings, edit `.github/workflows/backend-ci.yml`, `.github/workflows/frontend-ci.yml`, or `.github/workflows/selenium.yml`.
